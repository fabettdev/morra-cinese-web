import { StyleSheet } from "react-native";

export const leaderboardButtonStyle = StyleSheet.create({
    wrapper: {
        position: "fixed",
        top: 20,
        right: 50,
        flexDirection: "row",
        alignItems: "center"
    },
    image: {
        width: 120,
        height: 120,
    },
})